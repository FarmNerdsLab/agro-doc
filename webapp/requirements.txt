flask
# google-cloud-vision
gunicorn
Werkzeug
# torch will be installed separately in Dockerfile from PyTorch CPU index
# flash_attn==2.5.8  # GPU-only, requires CUDA compilation - not needed for CPU inference
accelerate==0.31.0
transformers==4.41.2